// import { somar } from "../src/calc"

// // Pode ser usado calc.test.ts
// Arquivo calc.ts excluído

// test('Deve somar corretamente', () => {
//   const resultado = somar(12, 20)
//   expect(resultado).toBe(32)
// })

test('10 + 10 = 20', () => {
  expect(10 + 10).toBe(20)
})